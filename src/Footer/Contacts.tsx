import { Component, ReactNode } from "react";

class Contacts extends Component {
  render(): ReactNode {
    return (
      <ul className="mx-72 my-8">
        <strong>Contacts</strong>
      </ul>
    );
  }
}

export default Contacts;
